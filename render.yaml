services:
  - type: web
    name: youtube-downloader
    env: node
    buildCommand: ./build.sh
    startCommand: node server.js
    disk:
      name: downloader-data
      mountPath: /data
      sizeGB: 10
